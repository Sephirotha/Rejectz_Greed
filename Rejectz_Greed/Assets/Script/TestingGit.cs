using System.Collections;
using System.Collections.Generic;
using UnityEngine;

public class TestingGit : MonoBehaviour
{
    private bool bl_Something = true;
    // Start is called before the first frame update
    void Start()
    {
        bl_Something = false;
    }

    // Update is called once per frame
    void Update()
    {
        
    }
}
